# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.32.0-standalone-strict/ingress-networking.k8s.io-v1.json
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lan-gateway-router
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/hostname: router.${BASE_DOMAIN}
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/upstream-vhost: "${ROUTER_IP}"
    nginx.ingress.kubernetes.io/proxy-redirect-from: "http://${ROUTER_IP}"
    nginx.ingress.kubernetes.io/proxy-redirect-to: "https://router.${BASE_DOMAIN}"
spec:
  ingressClassName: nginx
  rules:
    - host: router.${BASE_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: router-lan
                port:
                  number: 80
  tls:
    - secretName: router-tls
      hosts:
        - router.${BASE_DOMAIN}
