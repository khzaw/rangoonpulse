# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.32.0-standalone-strict/service-v1.json
---
apiVersion: v1
kind: Service
metadata:
  name: external-dns-canary
  namespace: monitoring
  annotations:
    # Temporary GitOps canary for validating the external-dns write path end-to-end.
    external-dns.alpha.kubernetes.io/hostname: extdns-canary.${BASE_DOMAIN}
spec:
  type: ExternalName
  externalName: grafana.${BASE_DOMAIN}
