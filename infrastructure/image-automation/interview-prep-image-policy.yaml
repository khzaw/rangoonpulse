---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: interview-prep
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: interview-prep
  filterTags:
    pattern: '^(?P<ts>\d+)-[a-f0-9]{40}$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
