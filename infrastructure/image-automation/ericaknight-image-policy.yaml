---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: ericaknight
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ericaknight
  filterTags:
    pattern: '^(?P<ts>\d+)-[a-f0-9]{40}$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
