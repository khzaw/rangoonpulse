---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: interview-prep
  namespace: flux-system
spec:
  image: ghcr.io/khzaw/interview-prep
  # Temporarily accelerated while the app is under active development.
  interval: 1h
