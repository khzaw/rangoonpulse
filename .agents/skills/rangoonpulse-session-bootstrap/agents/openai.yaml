interface:
  display_name: "Rangoonpulse Session Bootstrap"
  short_description: "Mandatory startup reading for rangoonpulse"
  default_prompt: "Always use $rangoonpulse-session-bootstrap first in /Users/khz/Code/rangoonpulse. Read AGENTS.md, README.md, docs/README.md, then the smallest relevant focused docs before planning, answering repo questions, review, debugging, or edits. After that, carry $rangoonpulse-shared-worktree for the rest of the session."
policy:
  allow_implicit_invocation: true
