interface:
  display_name: "Rangoonpulse Shared Worktree"
  short_description: "Mandatory shared-tree discipline for rangoonpulse"
  default_prompt: "Always use $rangoonpulse-shared-worktree in /Users/khz/Code/rangoonpulse after bootstrap reading. Expect concurrent edits, do not touch unrelated changes, stage only your hunks, commit on the current branch, push, and verify."
policy:
  allow_implicit_invocation: true
