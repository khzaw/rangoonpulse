apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seventyfivesoft
  namespace: default
spec:
  interval: 24h
  timeout: 10m
  chart:
    spec:
      chart: app-template
      version: "5.1.0"
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  install: {remediation: {retries: 3}}
  upgrade: {remediation: {retries: 3}}
  values:
    defaultPodOptionsStrategy: merge
    defaultPodOptions:
      nodeSelector:
        kubernetes.io/hostname: ${PRIMARY_NODE_NAME}
      terminationGracePeriodSeconds: 45
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
    controllers:
      main:
        strategy: Recreate
        replicas: 1
        initContainers:
          restore:
            image:
              repository: docker.io/litestream/litestream
              tag: 0.5.16
            command:
              - /usr/local/bin/litestream
            args:
              - restore
              - -if-db-not-exists
              - -if-replica-exists
              - -integrity-check
              - quick
              - -config
              - /etc/litestream.yml
              - /data/seventyfivesoft.sqlite
            resources:
              requests:
                cpu: "5m"
                memory: "16Mi"
              limits:
                cpu: "100m"
                memory: "64Mi"
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
        containers:
          main:
            image:
              repository: ghcr.io/seinhtettan/seventyfivesoft
              tag: sha-2472c1710bf1ee46e8099a0b9e73d5ba8fd2a699
              pullPolicy: IfNotPresent
            env:
              DATABASE_PATH: /data/seventyfivesoft.sqlite
              STATIC_ROOT: /app/dist
              PORT: "8080"
              TZ: "${TIMEZONE}"
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 8080
                  periodSeconds: 30
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 8080
                  periodSeconds: 10
            resources:
              requests:
                cpu: "25m"
                memory: "64Mi"
              limits:
                cpu: "500m"
                memory: "256Mi"
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
          litestream:
            image:
              repository: docker.io/litestream/litestream
              tag: 0.5.16
            command:
              - /usr/local/bin/litestream
            args:
              - replicate
              - -config
              - /etc/litestream.yml
            resources:
              requests:
                cpu: "5m"
                memory: "16Mi"
              limits:
                cpu: "100m"
                memory: "64Mi"
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
    service:
      main:
        controller: main
        type: ClusterIP
        ports:
          http:
            port: 8080
          metrics:
            port: 9090
    ingress:
      main:
        enabled: true
        className: nginx
        annotations:
          external-dns.alpha.kubernetes.io/hostname: seventyfivesoft.${ERICAKNIGHT_DOMAIN}
          nginx.ingress.kubernetes.io/ssl-redirect: "true"
          cert-manager.io/cluster-issuer: letsencrypt-prod
        hosts:
          - host: seventyfivesoft.${ERICAKNIGHT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: main
                  port: http
        tls:
          - secretName: seventyfivesoft-tls
            hosts:
              - seventyfivesoft.${ERICAKNIGHT_DOMAIN}
    configMaps:
      litestream:
        enabled: true
        data:
          litestream.yml: |
            addr: ":9090"
            logging:
              level: info
              type: json
            sync-interval: 5s
            snapshot:
              interval: 24h
              retention: 2160h
            validation:
              interval: 6h
            verify-compaction: true
            shutdown-sync-timeout: 30s
            dbs:
              - path: /data/seventyfivesoft.sqlite
                restore-if-db-not-exists: true
                monitor-interval: 1s
                checkpoint-interval: 1m
                replica:
                  type: file
                  path: /backup/seventyfivesoft
    persistence:
      data:
        enabled: true
        type: persistentVolumeClaim
        storageClass: local-path
        accessMode: ReadWriteOnce
        size: 1Gi
        advancedMounts:
          main:
            main:
              - path: /data
            litestream:
              - path: /data
            restore:
              - path: /data
      backup:
        enabled: true
        type: persistentVolumeClaim
        storageClass: truenas-nfs
        accessMode: ReadWriteOnce
        size: 1Gi
        advancedMounts:
          main:
            litestream:
              - path: /backup
            restore:
              - path: /backup
      litestream:
        enabled: true
        type: configMap
        identifier: litestream
        advancedMounts:
          main:
            litestream:
              - path: /etc/litestream.yml
                subPath: litestream.yml
                readOnly: true
            restore:
              - path: /etc/litestream.yml
                subPath: litestream.yml
                readOnly: true
      tmp:
        enabled: true
        type: emptyDir
        advancedMounts:
          main:
            main:
              - path: /tmp
            litestream:
              - path: /tmp
            restore:
              - path: /tmp