apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: transmission-vpn-ui
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: torrent-vpn.khzaw.dev
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - secretName: transmission-vpn-ui-tls
      hosts:
        - torrent-vpn.khzaw.dev
  rules:
    - host: torrent-vpn.khzaw.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: transmission-vpn-ui
                port:
                  number: 3000
