---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-secondary-data
  namespace: default
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
