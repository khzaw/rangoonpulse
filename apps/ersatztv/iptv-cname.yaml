# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.32.0-standalone-strict/service-v1.json
---
apiVersion: v1
kind: Service
metadata:
  name: ersatztv-iptv-cname
  namespace: default
  annotations:
    # Publish iptv.${BASE_DOMAIN} as a CNAME to ersatztv.${BASE_DOMAIN}.
    # external-dns must watch Services (`--source=service`) for this to work.
    external-dns.alpha.kubernetes.io/hostname: iptv.${BASE_DOMAIN}
spec:
  type: ExternalName
  externalName: ersatztv.${BASE_DOMAIN}
